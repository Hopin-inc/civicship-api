# Queries/Mutations-related type definitions

input ApplicationConfirmationInput {
    isApproved: Boolean!
    applicationId: String!
    confirmerId: String!
    comment: String
}

# Type definitions

type ApplicationConfirmationCreateSuccess {
    applicationConfirmation: ApplicationConfirmation!
}

# union definitions

union ApplicationConfirmationCreatePayload =
    ApplicationConfirmationCreateSuccess |
    InvalidInputValueError |
    AuthError |
    ComplexQueryError