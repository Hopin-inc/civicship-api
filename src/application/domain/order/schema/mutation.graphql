extend type Mutation {
  orderCreate(productId: ID!): OrderCreatePayload!
#  @authz(rules: [IsUser])
}

type OrderCreateSuccess {
  paymentLink: String!
}

union OrderCreatePayload = OrderCreateSuccess