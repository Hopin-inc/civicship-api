extend type Mutation {
  orderCreate(productId: ID!, paymentProvider: PaymentProvider = NMKR): OrderCreatePayload!
#  @authz(rules: [IsUser])
}

type OrderCreateSuccess {
  paymentLink: String!
}

union OrderCreatePayload = OrderCreateSuccess
