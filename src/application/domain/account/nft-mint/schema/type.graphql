type NftMint {
  id: ID!
  status: NftMintStatus!
  txHash: String
  error: String
  orderItemId: ID
  orderItem: OrderItem
  nftWalletId: ID!
  nftWallet: NftWallet!
  nftInstance: NftInstance
  createdAt: Datetime!
  updatedAt: Datetime!
}

enum NftMintStatus {
  QUEUED
  SUBMITTED
  MINTED
  FAILED
}
