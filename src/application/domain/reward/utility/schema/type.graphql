# ------------------------------
# Utility Object Type Definitions
# ------------------------------
type Utility {
    id: ID!
    name: String

    publishStatus: PublishStatus!
    pointsRequired: Int!

    description: String
    images: [String!]

    community: Community

    ticketIssuers: [TicketIssuer!]
    tickets: [Ticket!]

    requiredForOpportunities: [Opportunity!]

    createdByUser: User

    createdAt: Datetime
    updatedAt: Datetime
}
