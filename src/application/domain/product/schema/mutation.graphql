extend type Mutation {
    productBuy(productId: ID!, paymentProvider: PaymentProvider): ProductBuyPayload!
    #  @authz(rules: [IsUser])
}

type ProductBuySuccess {
    paymentLink: String!
}

union ProductBuyPayload = ProductBuySuccess
