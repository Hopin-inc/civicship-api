// import { and } from "graphql-shield";
// import { ShieldRule } from "graphql-shield/typings/types";
//
// const articleMutationPermissions: Record<string, ShieldRule> = {
//   articleCreate: and(isSelf, sanitizeInput),
//   articleUpdate: and(isSelf, sanitizeInput),
//   articleDelete: and(isSelf, sanitizeInput),
// };
//
// export { articleMutationPermissions };
