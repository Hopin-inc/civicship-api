# ------------------------------
# Utility Object Type Definitions
# ------------------------------
type Utility {
    id: ID!
    name: String!
    description: String
    image: String
    pointsRequired: Int!
    publishStatus: PublishStatus!

    community: Community!

    tickets: [Ticket!]
    requiredForOpportunities: [Opportunity!]

    createdAt: Datetime!
    updatedAt: Datetime
}
