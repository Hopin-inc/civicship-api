# ------------------------------
# Utility Object Type Definitions
# ------------------------------
type Utility {
    id: ID!
    name: String!
    description: String
    image: String
    pointsRequired: Int!
    publishStatus: PublishStatus!

    community: Community!
    # TODO connection?
    tickets: [Ticket!]
    requiredForOpportunities: [Opportunity!]

    createdAt: Datetime!
    updatedAt: Datetime
}
