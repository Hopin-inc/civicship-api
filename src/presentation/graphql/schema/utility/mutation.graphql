# Input definitions

input UtilityCreateInput {
    communityId: String!

    name: String!
    description: String
    image: ImageInput
    pointsRequired: Int!
}

input UtilityUpdateInfoInput {
    communityId: String!

    name: String!
    description: String
    image: ImageInput
    pointsRequired: Int!
}

input UtilityRedeemedInput {
    communityId: String!

    userWalletId: String!
}

input UtilityUseInput {
    communityId: String!

    userWalletId: String!
}

# Type definitions

type UtilityCreateSuccess {
    utility: Utility!
}

type UtilityDeleteSuccess {
    utilityId: String!
}

type UtilityUpdateInfoSuccess {
    utility: Utility!
}

type UtilityRedeemedSuccess {
    transaction: Transaction!
}

type UtilityUseSuccess {
    utility: Utility!
}

# Union definitions

union UtilityCreatePayload =
    UtilityCreateSuccess |
    InvalidInputValueError |
    AuthError |
    ComplexQueryError

union UtilityDeletePayload =
    UtilityDeleteSuccess |
    InvalidInputValueError |
    AuthError |
    ComplexQueryError

union UtilityUpdateInfoPayload =
    UtilityUpdateInfoSuccess |
    InvalidInputValueError |
    AuthError |
    ComplexQueryError

union UtilityRedeemedPayload =
    UtilityRedeemedSuccess |
    InvalidInputValueError |
    AuthError |
    ComplexQueryError

union UtilityUsePayload =
    UtilityUseSuccess |
    InvalidInputValueError |
    AuthError |
    ComplexQueryError