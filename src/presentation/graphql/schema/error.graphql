enum ErrorCode {
    VALIDATION_ERROR
    UNAUTHENTICATED
    FORBIDDEN
    NOT_FOUND
    INTERNAL_SERVER_ERROR
    RATE_LIMIT

    INSUFFICIENT_BALANCE
    INVALID_TRANSFER_METHOD
    MISSING_WALLET_INFORMATION
    UNSUPPORTED_TRANSACTION_REASON

    RESERVATION_FULL
    ALREADY_STARTED_RESERVATION
    RESERVATION_CANCELLATION_TIMEOUT
    RESERVATION_ADVANCE_BOOKING_REQUIRED
    RESERVATION_NOT_ACCEPTED
    SLOT_NOT_SCHEDULED
    TICKET_PARTICIPANT_MISMATCH

    ALREADY_JOINED
    NO_AVAILABLE_PARTICIPATION_SLOTS
    PERSONAL_RECORD_ONLY_DELETABLE

    ALREADY_EVALUATED
    CANNOT_EVALUATE_BEFORE_OPPORTUNITY_START

    ALREADY_USED_CLAIM_LINK
    CLAIM_LINK_EXPIRED

    # Order Processing Errors
    INVENTORY_UNAVAILABLE
    PAYMENT_SESSION_CREATION_FAILED
    ORDER_CANCELLATION_FAILED
    WEBHOOK_METADATA_INVALID
    NMKR_MINTING_FAILED
    NMKR_TOKEN_UNAVAILABLE
    NMKR_INSUFFICIENT_CREDITS
    PAYMENT_STATE_TRANSITION_FAILED
    
    # Inventory Calculation Errors
    PRODUCT_NOT_FOUND
    INVENTORY_CALCULATION_ERROR
    OVERSELL_DETECTED
    INSUFFICIENT_INVENTORY

    UNKNOWN
}

type Error {
    message: String!
    code: ErrorCode!
}
